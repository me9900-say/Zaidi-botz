/**
 * auto-typing.js
 * Note: Auto typing/recording is now handled centrally in index.js.
 * This plugin is kept for the toggle commands only.
 */
const { cmd } = require('../command');
const config = require('../config');

// These toggle commands are handled by settings.js
// This file is intentionally minimal to avoid duplicate presence updates.
